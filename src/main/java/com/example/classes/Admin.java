package com.example.classes;

public class Admin extends User{
    public Admin(String name, String surname, String middleName, String login, String password, int cardNumber, int cardPin) {
        super(name, surname, middleName, login, password, cardNumber, cardPin);
    }
}
